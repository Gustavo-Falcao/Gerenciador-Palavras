/*
import { setStateCards, setStatePagBusca } from "../state/State.js";

export function carregarCards() {
    const listStorage = localStorage.getItem('arrayCards');
    if(listStorage) {
    let cardsCarregados = JSON.parse(listStorage);
        setStatePagBusca({cards: cardsCarregados});
    }
}

export function atualizarCards() {
    const listStorage = localStorage.getItem('arrayCards');
    if(listStorage) {
    let cardsCarregados = JSON.parse(listStorage);
        setStateCards(cardsCarregados);
    }
}
    */